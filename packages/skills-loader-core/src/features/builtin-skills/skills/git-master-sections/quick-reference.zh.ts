export const GIT_MASTER_QUICK_REFERENCE_SECTION_ZH = `## 快速参考

### 风格检测速查表

| 如果 git log 显示... | 使用此风格 |
|---------------------|----------------|
| \`feat: xxx\`、\`fix: yyy\` | SEMANTIC |
| \`Add xxx\`、\`Fix yyy\`、\`添加 xxx\`、\`xxx追加\` | PLAIN |
| \`format\`、\`lint\`、\`typo\` | SHORT |
| 完整句子 | SENTENCE |
| 以上混合 | 使用多数（不要默认 semantic） |

### 决策树

\`\`\`
这是否在 main/master 上？
  是 -> NEW_COMMITS_ONLY，永不重写
  否 -> 继续

所有提交都是本地的（未推送）？
  是 -> 允许 AGGRESSIVE_REWRITE
  否 -> CAREFUL_REWRITE（force push 时警告）

改动是否补充已有提交？
  是 -> FIXUP 到那个提交
  否 -> 新提交

历史是否混乱？
  是 + 全本地 -> 考虑 RESET_REBUILD
  否 -> 正常流程
\`\`\`

### 反模式（自动失败）

1. **永远不要做一个巨型提交** - 3+ 文件必须 2+ 提交
2. **永远不要默认 semantic 风格** - 先从 git log 检测
3. **永远不要将测试与实现分离** - 始终同一提交
4. **永远不要按文件类型分组** - 按功能/模块分组
5. **永远不要重写已推送的历史**（未经明确许可）
6. **永远不要让工作目录脏着** - 完成所有改动
7. **永远不要跳过理由说明** - 解释文件为何分组
8. **永远不要使用模糊的分组理由** - "与 X 相关" 无效

---

## 执行前最终检查（阻塞）

\`\`\`
停下来验证 - 所有框都勾选前不要继续：

[] 文件数检查：N 个文件 -> 至少 ceil(N/3) 次提交？
  - 3 个文件 -> 最少 1 次提交
  - 5 个文件 -> 最少 2 次提交
  - 10 个文件 -> 最少 4 次提交
  - 20 个文件 -> 最少 7 次提交

[] 理由检查：对每个含 3+ 文件的提交，我写了原因吗？

[] 目录拆分检查：不同目录 -> 不同提交？

[] 测试配对检查：每个测试与其实现在一起？

[] 依赖顺序检查：基础在依赖之前？
\`\`\`

**硬性停止条件：**
- 从 3+ 文件做 1 次提交 -> **错了。拆分。**
- 从 10+ 文件做 2 次提交 -> **错了。继续拆分。**
- 无法用一句话说明文件分组理由 -> **错了。拆分。**
- 同一提交中有不同目录（无理由）-> **错了。拆分。**

---

### 提交模式
- 多文件一次提交 -> 拆分
- 默认 semantic 风格 -> 先检测

### Rebase 模式
- rebase main/master -> 永不
- 用 \`--force\` 而非 \`--force-with-lease\` -> 危险
- 不暂存脏文件就 rebase -> 会失败

### 历史搜索模式
- 该用 \`-G\` 时用 \`-S\` -> 结果错误
- 对移动的代码 blame 不加 \`-C\` -> 归因错误
- bisect 没有正确的 good/bad 边界 -> 浪费时间`
